#include <bits/stdc++.h>
using namespace std;
const int maxn = 1e4 + 5;
char str[maxn], ptr[maxn];
int Q, f[maxn], ans[maxn];
int main() {
    scanf("%d%s", &Q, str + 1);
    int i = 0;
    for(int t = 1; t <= Q; t ++) {
        if(str[t] >= 'a' and str[t] <= 'z') {
            ptr[++ i] = str[t];
            for(int j = )
        }
        else {

        }
    }
    return 0;
}