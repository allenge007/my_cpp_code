#include <bits/stdc++.h>

using namespace std;
const int maxn = 1e5 + 5;
int b[maxn];
void solve() {
    int n;
    scanf("%d", &n);
    for(int i = 1; i < n; i ++) scanf("%d", &b[i]);
    
}
int main() {
    int T;
    scanf("%d", &T);
    while(T --) solve();
    return 0;
}