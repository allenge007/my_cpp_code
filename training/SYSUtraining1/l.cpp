#include <bits/stdc++.h>

using namespace std;
const int maxn = 2e5 + 5;
int n;
struct node {
    int x, y;
}
int main() {
    scanf("%d", &n);
    for(int i = 1; i <= n; i ++) {
        char op[2];
        scanf("%s", op);
        if(op[0] == 'A') {

        }
        else {

        }
    }
    return 0;
}