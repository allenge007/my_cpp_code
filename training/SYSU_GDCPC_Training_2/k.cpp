#include <bits/stdc++.h>

using namespace std;

int main() {
    int a, b;
    cin >> a >> b;
    if(a > b) cout << "orz teralem is the king!";
    else if(a == b) cout << "even even seven EIeven.";
    else cout << "orz overflowker is the king!";
    return 0;
}