#include <bits/stdc++.h>
using namespace std;

const int N = 2010;

bitset<N> g[N];

int T, n, l, q;

void solve() {
    scanf("%d%d%d", &n, &l, &q);
    for (int i = 1; i <= l; i++) {
        
    }
}

int main() {
    cin >> T;
    while (T--) solve();
    return 0;
}