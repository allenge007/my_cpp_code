#include <bits/stdc++.h>

using namespace std;
const int maxn = 1e5 + 5;
int n, k, q;
void solve() {
    scanf("%d%d%d", &n, &k, &q);
}
int main() {
    int T;
    scanf("%d", &T);
    while(T --) solve();
    return 0;
}